using System.Collections;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using UnityEngine;

namespace ImageTools{

	public class ImageToolsManager {
		public void cvMat2Tex (){/*TODO*/}
		public void Tex2cvMat (){/*TODO*/}

		/*
		[DllImport("libImageTool.so")]
		public extern void split(cv::Mat input, cv::Mat outputLeft, cv::Mat outputRight);

		[DllImport("libImageTool.so")]
		public extern void blur(cv::Mat image, int kernel_size);
		[DllImport("libImageTool.so")]


		[DllImport("libImageTool.so")]
		void sobel(cv::Mat image, int kernel_size, int scale);

		[DllImport("libImageTool.so")]
		void canny(cv::Mat image, int kernel_size, double threshold, int ratio);
		*/
	}
}